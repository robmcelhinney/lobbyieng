[project]
name = "lobbyieng-data-tools"
version = "0.1.0"
description = "Data ingestion utilities for lobbyieng"
requires-python = ">=3.8"
dependencies = ["requests", "sqlalchemy"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
